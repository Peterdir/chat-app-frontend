<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="16dp"
    android:gravity="center_horizontal"
    android:orientation="vertical">

        <FrameLayout
            android:id="@+id/container_effect"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:background="@drawable/bg_pill_dark"
            android:backgroundTint="#1E1F22">

            <!-- Main Image (Visual of the effect) -->
            <ImageView
                android:id="@+id/img_effect_thumbnail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop" />

            <!-- None Icon -->
            <ImageView
                android:id="@+id/img_none_icon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_clear"
                app:tint="@color/discord_text_secondary"
                android:visibility="gone" />

            <!-- Shop Icon -->
            <ImageView
                android:id="@+id/img_shop_icon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_shop"
                app:tint="@color/white"
                android:visibility="gone" />

            <!-- New Badge for Shop -->
            <TextView
                android:id="@+id/txt_new_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:layout_marginTop="-8dp"
                android:layout_marginEnd="-4dp"
                android:background="@drawable/bg_new_badge"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp"
                android:text="MỚI"
                android:textColor="@android:color/white"
                android:textSize="10sp"
                android:textStyle="bold"
                android:visibility="gone" />

            <!-- Selection Border (Blue Glow Style) -->
            <View
                android:id="@+id/view_selection_border"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_selected_outline"
                android:visibility="gone" />

            <ImageView
                android:id="@+id/img_lock_effect"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="bottom|end"
                android:layout_margin="6dp"
                android:contentDescription="Khóa"
                android:src="@drawable/ic_lock"
                android:visibility="gone"
                app:tint="@color/white" />
        </FrameLayout>

        <TextView
            android:id="@+id/txt_effect_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Không"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            android:visibility="gone" />

    </LinearLayout>
