package com.example.chat_app_frontend.utils;

/**
 * DataSeeder – đã ngừng sử dụng.
 *
 * Trước đây class này tạo user test với mật khẩu plain-text trong Firebase RTDB.
 * Sau khi chuyển sang Firebase Authentication, credentials do Firebase quản lý
 * và không cần seed thủ công nữa.
 *
 * Tạo tài khoản test: vào màn hình Register và đăng ký bình thường.
 */
public class DataSeeder {
    // Intentionally empty – authentication is now handled by Firebase Auth.
}
